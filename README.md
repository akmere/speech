This program is based on PyTorch Lightning. It uses **uv** for package management.

**Model**
Lightning modules are located in **model.py**. Data modules are located in **dataset.py**. The file for execution is **train.py**. Some additional utility functions are defined in **util.py**.

```
uv run train.py --help
```
